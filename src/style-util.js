/**
 * 方正
 * 这个文件是一些工具类，并不是很重要，看不懂可以不理
 */


/**
 * 垂直剧中
 * @param {object} style
 */
export const centralization = style => ({
    ...style,
    alignItems: 'center',
    justifyContent: 'center'
})
